# Base Model

The base model is the foundation of an agent. It is a pre-trained large-scale language model that provides basic language understanding and generation capabilities for the agent. Mo AI Studio supports various advanced base models such as GPT-4, Claude, and more.